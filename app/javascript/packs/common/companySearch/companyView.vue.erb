<template lang="pug">
  div
    template(v-if='hasData')
      .ui.segment.shadow-less.with-top-margin
        h3.ui.header <%= I18n.t('common_vuejs.company_search.company_found') %>
        table.full-width
          tbody.small-horizontal-padding
            tr
              td.collapsing
                .small.text.bold <%= I18n.t('activerecord.attributes.company.name') %>
              td
                .small.text {{companyData.name}}
            tr
              td.collapsing
                .small.text.bold <%= I18n.t('common_vuejs.company_search.location') %>
              td
                .small.text {{companyData.location}}
          .ui.right.internal.rail.small-width
            button.ui.yellow.tiny.basic.button.no-margin(@click='detailButtonClicked')
              | <%= I18n.t('common_vuejs.company_search.show_detailed_information') %>
</template>

<script>
export default {
    name: 'company-view',
    props: ['companyData'],
    computed: {
        hasData: function() {
           return Boolean(this.companyData.siret)
        }
    },
    methods: {
        detailButtonClicked: function () {
            this.$emit('detail-button-click', this.companyData.siret)
        }
    }
}
</script>

<style lang="sass">
  .ui.segment.shadow-less.with-top-margin
    margin-top: 1em
  .ui.internal.rail.small-width
    width: auto
    padding: 1em
    margin: 0 0 0 1em
</style>