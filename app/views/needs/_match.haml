.needs-match
  - if match.person.present?
    = person_block(match.person, suffix: match.assistance&.title)
  - else
    = person_block_raw(prefix: t('.deleted_expert'),
                       name: match.expert_full_name,
                       suffix: match.assistance_title,
                       role: match.expert_institution_name)

  .match-status
    = match_status_label(match.status)

- if for_me
  %h4.ui.header
    = I18n.t("activerecord.attributes.match.statuses.#{match.status}")
    .sub.header= match.status_date

  = match_actions match
