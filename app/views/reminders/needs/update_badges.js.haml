- html = ''
- @need.badges.each do |badge|
  - html += "#{badge_label(badge)}"

var $div = document.getElementById('need-#{@need.id}-badges')
var $form = document.getElementById('need-#{@need.id}-form-badges')

if ($div !== undefined) {
$div.innerHTML = '#{html.html_safe}'
$div.classList.remove('hide')
}
if ($form !== undefined ) {
$form.classList.add('hide')
}
