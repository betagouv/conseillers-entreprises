<%- headers = ['period'] + @stats.first[1].keys -%>
<%= CSV.generate_line headers -%>
<%- @stats.reverse_each do |date_range, info| -%>
<%- row = [ date_range.begin.strftime('%Y-%m') ] + info.values.map(&:count) -%>
<%= CSV.generate_line row -%>
<%- end -%>
