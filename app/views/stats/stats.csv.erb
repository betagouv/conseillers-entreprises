<%-
headers = @stats.first[1].keys
if headers.first.is_a?(Range)
  headers = headers.map{ |k| k.begin.strftime('%Y-%m') }
end
headers = ['period'] + headers
-%>
<%= CSV.generate_line headers -%>
<%- @stats.each do |date_range, info| -%>
<%- row = [ date_range.begin.strftime('%Y-%m') ] + info.values -%>
<%= CSV.generate_line row -%>
<%- end -%>
