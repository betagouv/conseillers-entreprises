- cache([stats, charts_names], expires_in: 1.hour) do
  #stats-charts
    - charts_names.each do |name|
      - data = stats.send(name.to_sym)
      = render 'stats/stats_chart', data: data, name: name
