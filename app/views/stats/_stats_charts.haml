- cache("stats_charts-#{stats.params}", expires_in: 1.hour) do
  #stats-charts
    - names = [:needs, :source, :matches, :advisors]
    - names.each do |name|
      = render 'stats_chart', stats: stats, name: name
