= form_with model: @expert do |f|
  = hidden_field_tag :update_context, :edit
  .rf-input-group
    = f.label :email, class: 'rf-label'
    = f.email_field :email, class: 'rf-input', disabled: 'disabled'
  - if @expert.team?
    %p.rf-mb-3w
      %span.blue.ri-information-fill
      = t('.team_email_explanation_html', email: ENV['APPLICATION_EMAIL'])

  .required.rf-input-group
    = f.label :phone_number, class: 'rf-label'
    = f.text_field :phone_number, placeholder: t('attributes.placeholder.phone_number'), class: 'rf-input', autocomplete: 'off', required: true

  .required.rf-input-group
    = f.label :full_name, t('activerecord.attributes.expert.full_name'), class: 'rf-label'
    = f.text_field :full_name, placeholder: t('attributes.placeholder.team_name'), class: 'rf-input',
      autocomplete: 'off', required: true
  %p.rf-mb-3w
    %span.blue.ri-information-fill
    = t('.team_name_explanation')

  .required.rf-input-group
    = f.label :role, class: 'rf-label'
    = f.text_field :role, placeholder: t('attributes.placeholder.role'), class: 'rf-input', autocomplete: 'off', required: true

  .rf-mb-3w
    %p.rf-mb-2v= t('.antenne_and_institution')
    .rf-callout.rf-p-3w
      %p.rf-mb-0
        #{t('activerecord.models.antenne.one')} :
        %b= @expert.antenne.name
      %p.rf-mb-0
        #{t('activerecord.models.institution.one')} :
        %b= @expert.institution.name
  - if @expert.antenne != @user.antenne
    %p
      %span.orange.ri-information-fill
      = t('.different_antenne_warning_html', email: ENV['APPLICATION_EMAIL'])

  .rf-mb-3w
    %p.rf-mb-2v= t('.team_members')
    .rf-callout.rf-p-3w
      %ul.list-unstyled.rf-p-0.rf-m-0
        - @expert.users.not_deleted.each do |member|
          %li.rf-mb-1v
            %span.rf-m-0.js-simple-tooltip{ "data-simpletooltip-content-id" => "tooltip-case-#{member.id}" }
              - if member == @user
                %span.blue.ri-user-fill{ title: t('users.thats_you') }
              - else
                %span.ri-user-fill
              = member.full_name
            .hidden{ id: "tooltip-case-#{member.id}" }
              = person_block(member)

  .actions
    = f.submit t('update'), class: 'rf-btn btn-green'
