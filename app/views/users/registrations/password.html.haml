= form_with model: @user, url: user_path, local: true, method: :put do |f|
  = render "devise/shared/error_messages", resource: @user
  .required.rf-input-group
    = f.label :current_password, class: 'rf-label'
    = f.password_field :current_password, required: true, autocomplete: 'current-password', class: 'rf-input'
  .required.rf-input-group
    = f.label :password, t('devise.passwords.edit.new_password'), class: 'rf-label'
    - placeholder = t('devise.password_minimum_characters', count: @minimum_password_length)
    = f.password_field :password, required: true, placeholder: placeholder, autocomplete: 'new-password', class: 'rf-input'
  .required.rf-input-group
    = f.label :password_confirmation, class: 'rf-label'
    = f.password_field :password_confirmation, required: true, autocomplete: 'new-password', class: 'rf-input'
  .actions
    = f.submit t('devise.passwords.edit.change_own_password'), class: 'rf-btn btn-green'
