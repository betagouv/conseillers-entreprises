- if landing_theme.logos.present?
  %h2.section__subtitle= t('.institutions')
  .logo-list
    = region_image(session[:region_code])
    - landing_theme.logos.split(',').each do |logo_slug|
      = institution_image(logo_slug)
