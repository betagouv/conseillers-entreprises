:ruby
  meta title: @landing_theme.meta_title, description: @landing_theme.meta_description

- cache_unless(flash.present?, [@landing_theme, in_iframe?]) do

  - unless in_iframe?
    %section.section.section-white.landing.less-padding
      .container
        .row.landing-header
          .title
            .heading
              -# - if @landing_theme.main_logo
              -#   = display_image(name: @landing_theme.main_logo)
              %h1= @landing_theme.title
            %p.subtitle= t('.catch_phrase')
            .links
              = render 'pages/cta_button'
          .image
            = image_tag('home.png', alt: "")

  -# - if @landing_theme.home_sort_order.present?
  -# = render 'pages/breadcrumbs', landing: @landing_theme

  - unless in_iframe?
    #scroll-section

  %section.section.section-grey#section-exemples
    .container
      %h2= @landing_theme.title.presence || t('you_wish')
      .landing-topics
        = render partial: 'landings/landing_subjects/card', collection: @landing_subjects, as: :landing_subject, locals: { landing: @landing }


      - unless in_iframe?
        .ui.text-center.other
          %b= t('.back_to_other_subjects').html_safe

  - unless in_iframe?
    %section.section.section-light-grey.landing
      = render 'pages/how_it_works'

    %section.section.section-white
      .container
        = render 'institutions', landing_theme: @landing_theme
