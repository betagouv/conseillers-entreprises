- meta title: t('.title', antenne: current_user.antenne.name)

%h1.ui.header= t('.title', antenne: current_user.antenne.name)

= render 'diagnoses', diagnoses: @diagnoses.in_progress, title: t('diagnoses.index.diagnoses_in_progress')

= render 'diagnoses', diagnoses: @diagnoses.completed, title: t('diagnoses.index.completed_diagnoses')

= link_to archives_antenne_diagnoses_path do
  %i.archive.icon
  = t('.browse_archived_diagnoses')
