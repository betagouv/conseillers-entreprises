- diagnoses.order!(happened_on: :desc)
.ui.segments.shadow-less.selectable-list
  .ui.segment.secondary
    .ui.header
      = title
      .sub.header= diagnoses.human_count
  = render diagnoses, needs_of_interest: local_assigns[:needs_of_interest]
