<%= t('mailers.hello') %>

<%= ActionView::Base.full_sanitizer.sanitize(t('.company_needs_you', subject: @need.subject)) %> :

<%= render 'mailers/expert_mailer/need_card', need: @need, visited_at: false %>

<% if @solicitation.present? && @solicitation.landing&.partner_full_url.present? %>
<%= ActionView::Base.full_sanitizer.sanitize(t('.via_partner_html', url: @solicitation.landing.partner_full_url)) %>
<% end %>

<%= t('mailers.thanks_for_your_trust') %>
<%= t('mailers.see_you_on_service_html', link_to_root: root_url, target: :blank) %>

---
<%= render 'mailers/expert_mailer/support_user_signature', support_user: @support_user %>

