= render 'mailers/header', institution_logo_name: @expert.institution.logo&.filename

%p= t('mailers.hello')

- link_to_root = link_to t('app_name'), root_url
%p= t('.needs_for_you', link_to_root: link_to_root)

= render 'mailers/expert_mailer/remind_received_needs',
  received_needs: @needs_quo,
  title: t('.needs_quo'),
  visited_at: false

%p= t('mailers.see_you_on_service_html', link_to_root: link_to_root)

.info
  %p= t('.support_contact_label')
  = render 'mailers/expert_mailer/support_contacts', antenne: @expert.antenne
