<%= t('mailers.hello') %>

<% link_to_root = link_to t('app_name'), root_url %>
<%= t('.needs_for_you', link_to_root: link_to_root) %>

<%= render 'mailers/expert_mailer/remind_received_needs',
  received_needs: @firsts_needs_quo,
  title: t('.needs_quo'),
  visited_at: false %>

<% if @others_needs_quo_count.positive? %>
<% account_link = link_to t('.user_account'), quo_active_needs_url %>
<%= ActionView::Base.full_sanitizer.sanitize(t('.other_needs_quo_html', count: @others_needs_quo_count, account_link: account_link)) %>
<% end %>


<%= ActionView::Base.full_sanitizer.sanitize(t('mailers.see_you_on_service_html', link_to_root: link_to_root)) %>

---
<%= render 'mailers/expert_mailer/support_user_signature', support_user: @support_user %>
