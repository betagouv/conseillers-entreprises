= render 'mailers/header'

%p= t('mailers.hello')

%p= t('mailers.solicitation_mailer.intro_html', link_to_root: link_to(t('app_name'), root_url), solicitation_subject: t('.solicitation_subject_html'))

%p= t('.interior_ministry_html')

%p= t('.phone_number_html')

%p= t('mailers.hope_to_helped')

= render 'mailers/solicitation_mailer/footer'
