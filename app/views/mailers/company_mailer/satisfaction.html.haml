:css
  .button {
    padding: 0.25em 1em;
    color: #fff;
    margin: 0 0.6em;
  }
  .no {
    background-color: #b92a1c;
  }
  .yes {
    background-color: rgb(0, 83, 179);
  }


%p= t('mailers.hello')

= t('.intro_html', company: @need.company.name)

%blockquote
  %strong= @need.subject.label

= t('.question_one_html', link: new_company_satisfactions_url(besoin: @need.id, token: @email_token))

= t('.question_two_html', link: new_company_satisfactions_url(besoin: @need.id, token: @email_token))

%p= t('.outro')

%p= t('.tank_you')

- link_to_root = link_to t('app_name'), root_url
%p= t('mailers.place_des_entreprises_team_html', link_to_root: link_to_root)
