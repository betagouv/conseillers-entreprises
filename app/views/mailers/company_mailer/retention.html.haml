- link_to_root = link_to t('app_name'), root_url(mtm_campaign: 'retention', mtm_kwd: 'mail-retention')

= render 'mailers/header'

%p= t('mailers.hello')

%p= t('.intro_html', company: @need.diagnosis.company.name)

%p
  %strong= @need.subject.label

%p= t('.remind')

%p= t('.list_html')

%p= t('.outro')

%p= t('mailers.thanks_for_your_trust')

%p= t('mailers.cordially')

%p= t('mailers.place_des_entreprises_team_html', link_to_root: link_to_root)

.button
  = link_to t('.send_solicitation'), root_url(mtm_campaign: 'retention', mtm_kwd: 'mail-retention')
