%h1.fr-h2= institution.name

%nav.fr-breadcrumb{ 'aria-label': t('shared.breadcrumb.you_are_here'), role: 'navigation' }
  %button.fr-breadcrumb__button{ 'aria-controls': 'breadcrumb-189', 'aria-expanded': 'false' }
  .fr-collapse#breadcrumb-189
    %ol.fr-breadcrumb__list
      %li
        = link_to Institution.model_name.human.pluralize, institutions_path, class: 'fr-breadcrumb__link'
      - if local_assigns[:antenne].present?
        %li
          = link_to institution.name, institution, class: 'fr-breadcrumb__link'
        - if local_assigns[:antenne].present?
          %li
            %a.fr-breadcrumb__link{ 'aria-current': 'page' }
              = local_assigns[:antenne]
      - else
        %li
          %a.fr-breadcrumb__link{ 'aria-current': 'page' }
            = institution.name
