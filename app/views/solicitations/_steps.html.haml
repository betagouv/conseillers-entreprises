%ul.steps-form
  - Solicitation.completion_steps.except(:completed).keys.each_with_index do |step, index|
    %li{ class: solicitation_step_class(index, current_step) }
      %div= t("solicitations.creation_form.steps.#{step}")
