= link_to path, class: 'fr-btn fr-btn--secondary', aria: { label: "#{t('solicitations.creation_form.previous')} - #{label}" } do
  %span.icon.ri-arrow-left-s-line{ 'aria-hidden': 'true' }
  %span= t('solicitations.creation_form.previous')
