- if alert || notice
  %p#flashes
    - if alert
      .ui.icon.error.message
        .icon.inbox
        %p= alert

    - if notice
      .ui.icon.success.message
        %i.success.icon
        .content
          .header
          %p= notice
