%li.panel.landing-topic
  %input{ :checked => "checked", :type => "checkbox", onchange: "sendLandingTopicEvent('#{landing_topic.title}')" }
  %i
  .panel__header
    %h3= simple_format(landing_topic.title, {}, wrapper_tag: 'span')
  .lead-text= landing_topic.description.html_safe
