- feedback_html = render 'feedbacks/feedback', feedback: @feedback, highlighted_experts: []
document.getElementById('feedback_form-#{@feedback.feedbackable_type}-#{@feedback.feedbackable_id}').insertAdjacentHTML('beforebegin', "#{j feedback_html}");

document.querySelector('#feedback_form-#{@feedback.feedbackable_type}-#{@feedback.feedbackable_id} .form').reset()
