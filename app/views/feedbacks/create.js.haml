var comment_html = "#{j feedback_block(@feedback, true)}";
document.getElementById("feedbacks-need-#{@feedback.need.id}").insertAdjacentHTML('beforeend', comment_html);
document.getElementById('feedback-form-#{@feedback.need.id}').reset()
