// Manifest file for the / page.
// We currently have basically two separate sites.
// * / (solicitations), based on the datagouv template
// * the rest of the app, using semantic-ui.
// This is hopefully a workaround until we migrate the rest of the app to the datagouv template

// = require rails-ujs
// = require turbolinks
